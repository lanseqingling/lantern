interface:
  display_name: "Create with Lantern"
  short_description: "通过 Lantern 受控领域能力管理和编辑漫画"
  default_prompt: "Use $create-with-lantern to work with my Lantern comics through the management and editing capabilities currently available."

dependencies:
  tools:
    - type: "mcp"
      value: "lantern"
      description: "Lantern application MCP for bounded comic context and registered creative capabilities"
      transport: "streamable_http"
      url: "http://127.0.0.1:18787/mcp"
